<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.android.print.demo"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.hardware.usb.host"/>
    <uses-permission android:name="android.permission.CHANGE_CONFIGURATION" tools:ignore="ProtectedPermissions" />
    <uses-permission-sdk-23 android:name="android.permission.ACCESS_COARSE_LOCATION"/><!-- Android6.0 蓝牙扫描才需要	-->

    <!-- Needed only if your app looks for Bluetooth devices. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS"/>
    <uses-permission android:name="android.permission.BLUETOOTH_PRIVILEGED"
        tools:ignore="ProtectedPermissions" />


    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:exported="true"
            android:configChanges="locale|layoutDirection|keyboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".TextEdit" android:configChanges="locale|layoutDirection|keyboard"></activity>

        <activity android:name=".RouterEdit" android:configChanges="locale|layoutDirection|keyboard"></activity>

        <activity android:name=".LanguageEdit" android:configChanges="locale|layoutDirection|keyboard"></activity>

        <activity android:name=".QREdit" android:configChanges="locale|layoutDirection|keyboard"></activity>

        <activity
            android:name=".bluetooth.BluetoothDeviceList"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Dialog"
            android:configChanges="locale|layoutDirection|keyboard"/>

        <activity
            android:name=".wifi.WifiDeviceList"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Dialog"
            android:configChanges="locale|layoutDirection|keyboard"/>

        <activity
            android:name=".usb.UsbDeviceList"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Dialog"
            android:configChanges="locale|layoutDirection|keyboard"/>

    </application>

</manifest>